# Starfit 领域语言

移动端优先的健身记录与 AI 教练应用（iOS + watchOS 伴侣 App）的领域术语表。

## 训练执行

**训练会话 (Workout Session)**:
一次完整训练的事实记录（L2），从开始到结束，包含全部动作与组数据。
_Avoid_: 训练记录、课表

**组 (Set)**:
训练的最小执行单元：一次重量×次数（或时长/距离）的尝试，有生命周期状态（PLANNED → COMPLETED / SKIPPED）。

**组平均心率 (Set Average HR)**:
单组执行期间的平均心率（bpm），写入组的单值字段。由心率样本聚合而来，不再接受手动录入。
_Avoid_: 手动心率、实时心率（实时是采集行为，不是数据）

## 心率数据

**心率样本 (HR Sample)**:
某一时刻的单次心率测量值（bpm + 时间戳 + 来源），存于 `heart_rate_samples` 时序表，是走势图与脚本分析的最小数据单元。入库分辨率：每 5 秒 1 个均值。
_Avoid_: 心率数据（口语，未指明粒度）、原始心率流

**心率真源 (HR Source of Truth)**:
HealthKit。本系统的样本库是分析级副本（5s 降采样），更细分辨率永远可从 HealthKit 重拉。
_Avoid_: 手表数据库（手表只是采集端，不是真源）

**负荷锚点 (Load Anchor)**:
用户档案中的静态生理基线（最大心率、静息心率、体重、力量水平），用于计算强度区间与偏差校验。区别于时序的心率样本。
_Avoid_: 心率指标（与样本混淆）

## 设备与同步

**手表伴侣 (Watch Companion)**:
watchOS App（部署目标 watchOS 26，UI 全面走 Liquid Glass 官方管线）。角色 = 镜像+遥控+心率采集，不是独立执行端。
_Avoid_: 手表端 App（未指明从属关系）

**手机中继 (Phone Relay)**:
WatchConnectivity 近场通路的手机侧角色：手表数据统一经手机进后端，手表不直连 API。
_Avoid_: 手表同步、直连同步

**心率低频推送 (HR Live Peek)**:
训练中手表每分钟推 1 个均值到手机做展示的通路。完整样本流训后批量同步。区别于样本入库。
_Avoid_: 实时同步（v1 不做秒级实时推送）

## 休息

**组间休息 (Rest)**:
组完成后的倒计时状态，手表与手机各有结束/加时交互。手机端为长按加 20 秒；手表端为双按钮（结束休息 / 加 10 秒）。
_Avoid_: 休息页面、当天休息
